diff --git a/haiou-live/assets/js/config.js b/haiou-live/assets/js/config.js
@@
-    state.cfg.rooms = apiData.rooms.map(function (r) {
+    state.cfg.rooms = apiData.rooms.map(function (r) {
@@
     state.cfg.roomsApiFailed = false;
     state.cfg.roomsLoadError = '';
+
+    // ⭐ 新增：把正在直播的房间排到数组最前面，其次按 sort 排序（2026-06-19）
+    state.cfg.rooms.sort(function (a, b) {
+      if (a.status === 'live' && b.status !== 'live') return -1;
+      if (a.status !== 'live' && b.status === 'live') return 1;
+      return (a.sort || 0) - (b.sort || 0);
+    });
@@
-diff --git a/haiou-live/assets/js/rooms.js b/haiou-live/assets/js/rooms.js
-@@
-   const cfg = state.cfg;
-   const heroRooms = cfg.rooms.slice(0, 5);
+diff --git a/haiou-live/assets/js/rooms.js b/haiou-live/assets/js/rooms.js
+@@
+   const cfg = state.cfg;
+   const liveRooms = cfg.rooms.filter(r => r.status === 'live');
+   const heroRooms = (liveRooms.length ? liveRooms : cfg.rooms).slice(0, 5);
+@@
+   loadPcHomeOdds();
+   initHomeReplays();
+
+   // ⭐ 新增：可选 30s 轮询刷新，需在 site-config.json 打开 heroAutoRefresh
+   if (state.cfg.heroAutoRefresh) {
+     setInterval(refreshHeroRooms, 30000);
+   }
+
+   function refreshHeroRooms() {
+     fetch('/api/public/rooms?t=' + Date.now())
+       .then(r => r.json())
+       .then(data => {
+         if (!data.ok || !Array.isArray(data.rooms)) return;
+         const live = data.rooms.filter(r => r.status === 'live');
+         if (!live.length) return;
+         const newIds = live.slice(0, 5).map(r => r.id).join(',');
+         const oldIds = heroRooms.map(r => r.id).join(',');
+         if (newIds === oldIds) return;
+         state.cfg.rooms = data.rooms;
+         rebuildHero();
+       })
+       .catch(console.warn);
+   }
+
+   function rebuildHero() {
+     const zone = document.querySelector('#heroPlayerZone');
+     const cardsWrap = document.querySelector('#heroSideCards');
+     const enterBtn = document.querySelector('#heroEnterBtn');
+     if (!zone || !cardsWrap) return;
+
+     const liveRooms = state.cfg.rooms.filter(r => r.status === 'live');
+     const heroRooms = (liveRooms.length ? liveRooms : state.cfg.rooms).slice(0, 5);
+
+     cardsWrap.innerHTML = heroRooms.map((r, i) => `
+       <div class="hero-side-card ${i === 0 ? 'is-active' : ''}" data-room-id="${r.id}" data-cover="${asset(r.cover)}" data-title="${esc(r.title)}">
+         <img src="${asset(r.cover)}" alt="${esc(r.title)}">
+       </div>`).join('');
+
+     if (heroRooms[0]) {
+       zone.style.backgroundImage = `url(${asset(heroRooms[0].cover)})`;
+       zone.dataset.roomId = heroRooms[0].id;
+       if (enterBtn) enterBtn.href = href('pages/room.html?id=' + heroRooms[0].id);
+       playHeroPreview(heroRooms[0]);
+     }
+
+     document.querySelectorAll('#heroSideCards .hero-side-card').forEach(card => {
+       card.addEventListener('click', function () {
+         const rid = this.dataset.roomId;
+         const cover = this.dataset.cover;
+         document.querySelectorAll('#heroSideCards .hero-side-card').forEach(c => c.classList.remove('is-active'));
+         this.classList.add('is-active');
+         zone.style.backgroundImage = `url(${cover})`;
+         zone.dataset.roomId = rid;
+         if (enterBtn) enterBtn.href = `pages/room.html?id=${rid}`;
+         const room = state.cfg.rooms.find(r => String(r.id) === String(rid));
+         if (room) playHeroPreview(room);
+       });
+     });
+   }
 