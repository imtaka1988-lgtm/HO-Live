# 海鸥直播 — 香港服务器部署说明

> 版本：V4
> 日期：2026-06-12
> 适用：Ubuntu 20.04/22.04 + Nginx

---

## 一、部署前需要准备的信息

> 以下信息由服务器管理员提供，先确认已具备再开始部署。

| 项目 | 说明 | 示例 |
|------|------|------|
| 服务器 IP | 香港服务器的公网 IPv4 | `203.0.113.5` |
| SSH 用户名 | 通常为 `root` 或自定义用户 | `root` |
| SSH 密码或密钥 | 登录方式 | 密码 或 `.pem` 私钥文件 |
| 网站域名 | 已解析到服务器 IP 的域名 | `www.haiou.live` |
| 部署目录 | 前端文件存放位置 | `/data/haiou-live/frontend` |
| Nginx 配置路径 | Nginx site 配置文件目录 | `/etc/nginx/sites-available/` |
| 是否启用 HTTPS | 是否需要申请 SSL 证书 | 是（推荐 Let's Encrypt） |

---

## 二、部署步骤

### 2.1 上传前端文件

```bash
scp -r live_site_yellow_style_full/* root@203.0.113.5:/data/haiou-live/frontend/
```

### 2.2 安装 Nginx

```bash
ssh root@203.0.113.5
apt update && apt install nginx -y
```

### 2.3 配置 Nginx

```bash
scp deploy/nginx/haiou-live-frontend.conf root@203.0.113.5:/etc/nginx/sites-available/
ssh root@203.0.113.5
nano /etc/nginx/sites-available/haiou-live-frontend.conf
# 修改 server_name 为实际域名
ln -s /etc/nginx/sites-available/haiou-live-frontend.conf /etc/nginx/sites-enabled/
nginx -t && systemctl reload nginx
```

### 2.4 HTTPS（可选，推荐）

```bash
ssh root@203.0.113.5
apt install certbot python3-certbot-nginx -y
certbot --nginx -d www.haiou.live
```

---

## 三、后续维护

**前端更新：**
```bash
scp -r live_site_yellow_style_full/* root@203.0.113.5:/data/haiou-live/frontend/
```

**后端 API 上线后**，编辑 Nginx 配置取消 `location /api/` 和 `location /ws/` 的注释，重载 Nginx。
