# 海鸥直播 / HO-Live

> 当前状态：V4 最终封包版（2026-06-14）

这是海鸥直播体育直播站代码仓库。当前版本已经完成安全、稳定性、后台房间管理、Nginx、聊天室连接限制、数据库配置保护和 Odds API 超时加固，可以作为当前线上稳定封包继续使用。

---

## 一、当前封包结论

可以按当前 `main` 分支作为线上版本使用。

最终稳定封包分支：

```text
stable-sealed-20260614
```

本轮修复重点：

- 后台登录、用户登录、注册接口限流
- HTML 转义和前端 URL 安全处理
- 图片上传真实文件头校验
- 删除房间时同步清理关联数据
- 新建房间补最小空缺 ID
- 房间列表按 ID 从小到大返回
- 主播重置密码随机源加固
- 直播回调接口增加 key 鉴权
- 减少公开健康检查接口信息暴露
- 去掉公开房间列表访问时改表逻辑
- 整理重复创建房间路由挂载
- Nginx CSP 删除未知外部域名
- Nginx `/api/` 增加请求体大小限制
- CORS 支持域名白名单配置
- WebSocket 聊天室增加单 IP / 总连接数限制
- 数据库密码缺失时拒绝启动，除非显式允许空密码
- 公开接口错误信息收敛，不再直接暴露内部错误
- Odds API 请求增加超时保护

---

## 二、服务器更新命令

常规上线：

```bash
cd /var/www
git pull origin main
pm2 restart all
nginx -t
systemctl reload nginx
```

如果 `nginx -t` 报错，不要执行 `systemctl reload nginx`，先修 Nginx 配置。

前端 JS 或页面更新后，浏览器建议强刷：

```text
Ctrl + F5
```

---

## 三、稳定备份与回退

最终稳定封包分支：

```text
stable-sealed-20260614
```

如果线上出现严重问题，优先回退到最终稳定封包：

```bash
cd /var/www
git fetch origin
git reset --hard origin/stable-sealed-20260614
pm2 restart all
nginx -t
systemctl reload nginx
```

原始稳定备份分支，也就是本轮修复前的备份：

```text
stable-delivery-20260614
```

原始稳定备份提交：

```text
d6b7c9fbf7f0e1e5d017341edd48caa601dd60f6
```

如果要回到本轮修复前，可以执行：

```bash
cd /var/www
git fetch origin
git reset --hard origin/stable-delivery-20260614
pm2 restart all
```

---

## 四、上线前必须确认的环境变量

数据库密码现在默认要求配置：

```bash
DB_PASS=你的数据库密码
```

如果你的数据库确实故意使用空密码，才可以临时设置：

```bash
ALLOW_EMPTY_DB_PASS=1
```

正式环境不建议空密码。

Odds API 默认 5 秒超时，可以按需调整：

```bash
ODDS_API_TIMEOUT_MS=5000
```

---

## 五、房间 ID 规则

当前规则：

```text
已有房间 ID 不自动改名。
删除 12 号房间后，13 号仍然保持 13。
下一次新建房间时，优先补回最小空缺 ID，也就是补回 12。
```

这样做比“删除 12 后立刻把 13 改成 12”更安全。

原因：房间 ID 会关联主播账号、播放源、推流配置、直播回调记录、后台按钮、前台直播间链接。强行改已有房间 ID 容易导致按钮错位、播放源错位、主播账号错位。

重点验收：

```text
1. 删除 12 号房间
2. 再新建房间
3. 新房间应补回 12
4. 房间列表应按 1、2、3、4... 从小到大排列
5. 每行四个按钮必须对应当前行房间
```

---

## 六、后台手动放视频源

后台手动填 m3u8 / flv 播放源不受直播回调鉴权影响。

你仍然可以按原方式操作：

```text
后台 → 房间 → 填播放源 → 保存 → 前台播放
```

影响范围说明：

```text
后台手动填播放源：不受影响
后台新增房间：不受影响
后台改房间资料：不受影响
用户看直播：不受影响
直播云厂商自动通知开播/关播：需要 key
```

---

## 七、直播回调接口 key

直播回调接口用于腾讯云/阿里云等直播服务通知网站：某个房间开播或关播。

现在该接口需要 key，防止外部伪造请求乱改房间状态。

服务器环境变量：

```bash
LIVE_CALLBACK_KEY=你自己设置的随机长字符串
```

云厂商回调地址示例：

```text
https://你的域名/api/live/callback?key=同一个随机长字符串
```

如果暂时不使用云直播回调，可以不用管。

如果临时要放开无 key 回调，可以设置：

```bash
LIVE_CALLBACK_ALLOW_UNSIGNED=1
```

正式环境不建议长期放开。

重点验收：

```text
1. OBS 推流后，房间状态是否自动变直播中
2. OBS 停推后，房间状态是否自动变离线
3. 如果没有配置 LIVE_CALLBACK_KEY，回调会被拒绝
4. 后台手动改状态仍然可以使用
```

---

## 八、CORS 与聊天室连接限制

CORS 默认保持原行为，不设置 `CORS_ORIGIN` 时不会限制来源，避免误伤当前线上。

如果要收紧跨域来源，可以设置：

```bash
CORS_ORIGIN=https://s6.lol,https://www.s6.lol
```

聊天室 WebSocket 默认限制：

```bash
CHAT_WS_MAX_CONNECTIONS_PER_IP=20
CHAT_WS_MAX_CONNECTIONS_TOTAL=1000
```

这两个值可以按实际流量调整。

---

## 九、重点验收清单

每次更新后，建议至少检查这些：

```text
1. 首页能正常打开
2. 直播间能正常打开
3. 后台能登录
4. 新建房间正常
5. 删除房间正常
6. 删除 12 后再新建能补回 12
7. 房间列表按 ID 从小到大排列
8. 每行四个操作按钮对应当前行房间
9. 上传封面、头像正常
10. 重置主播密码后可以登录主播后台
11. 手动填写 m3u8 / flv 播放源后前台能播放
12. 如果使用直播回调，key 配置正确
13. 聊天室能连接和发言
14. Nginx `nginx -t` 测试通过
15. 后台 Odds API 健康检查不会长时间卡住
```

---

## 十、本轮主要修复记录

已完成：

1. 稳定备份分支
2. HTML 转义修复
3. 禁止危险跳转链接
4. 登录限流
5. 新建主播密码随机源加固
6. 上传图片真实文件头校验
7. 健康检查接口减少信息暴露
8. 注册接口限流
9. API 基础安全响应头
10. 前端资源 URL 限制
11. 删除房间关联数据清理
12. 房间创建补空缺 ID
13. 房间列表按 ID 升序
14. 主播重置密码随机源加固
15. 重复创建房间路由挂载整理
16. 公开房间列表移除访问时改表
17. 直播回调接口 key 鉴权
18. 整体复查后补强 URL 工具大小写绕过
19. README 封包文档
20. Nginx CSP 删除未知外部域名
21. Nginx `/api/` 请求体大小限制
22. CORS 可配置白名单
23. WebSocket 单 IP / 总连接数限制
24. DB_PASS 缺失保护
25. 公开接口错误信息收敛
26. Odds API 请求超时保护

---

## 十一、当前不建议再动的地方

为了保持稳定，当前不建议继续大改：

- 播放器核心逻辑
- 前端整体版式
- 数据库表结构
- 聊天室核心逻辑
- 房间 ID 规则
- 后台四个操作按钮绑定方式

后续只做小修小补，避免破坏当前稳定版。
